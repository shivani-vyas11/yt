Developer tracking system

Functionality:

# Create a task
    - task_id: auto
    - task 
    - priority
    - deadline
    - status: default pending

# View a task
    - check if any task exists, check length, if not present: show msg
    - IF task present: show tasks
# Update
    - check length if tasks exist to update
    - ask for id of task 
    - check the id task existence : if present : add new task,
                                    else task not found w this id
    
# Delete
    - check len if exists
    - ask for id : if id present : ask confirmation 
                                    if yes : del
                    else : not found
# Priority
# Deadline

## practise performing sql like data querying queries in python, CRUD, sort viewing, order by and everything